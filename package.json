{
  "name": "marketplace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "apps/remotes/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n shell,header -c blue,magenta \"npm:dev:shell\" \"npm:dev:header\"",
    "dev:shell": "npm run dev -w @marketplace/shell",
    "dev:header": "npm run dev -w @marketplace/header",
    "typecheck": "npm run typecheck -ws --if-present",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}